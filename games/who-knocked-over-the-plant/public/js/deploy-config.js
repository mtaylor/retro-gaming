/**
 * After deploying the Node server (see README), paste your public URL here.
 * Example: 'https://who-knocked-over-the-plant.onrender.com'
 *
 * Leave empty to use same-origin (local npm start or opening the Render URL directly).
 */
export const REMOTE_SERVER_URL = '';
